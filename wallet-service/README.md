### Wallet Service

# Only this wallet-service has access to the actual money in the database

## Learned that balance is always calculated and never stored

## Learned and implemented to handle Idempotencty, double spending and race conditions during withdrawal, lock, release & settlement
